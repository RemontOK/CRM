RewriteEngine On

# Debug/maintenance scripts must not be reachable over HTTP
RewriteRule ^scripts/ - [F,L]

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
