# Production: leave unset in .env.production — app uses same-origin /api automatically.
# Local dev: put http://127.0.0.1:4001/api in .env.development.local (not .env.local).
# VITE_API_URL=https://your-domain.ru/api
